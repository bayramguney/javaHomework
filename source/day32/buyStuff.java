package day32;

import java.util.ArrayList;

public class buyStuff {
    public static void myList(ArrayList<String> items) {
        for (String item : items) {

            if (item.equals("milk")) {
                System.out.println("i go to store and buy " + item);
            } else if (item.equals(("bread"))) {
                System.out.println("i go to store and buy " + item);
            } else if (item.equals(("water"))) {
                System.out.println("i go to store and buy " + item);
            } else if (item.equals(("sugar"))) {
                System.out.println("i go to store and buy " + item);
            }


        }

    }
}

